# frozen_string_literal: true

require "test_helper"

class PostTest < ActiveSupport::TestCase
  test "Pending tests . . . Need to ensure that the commets table is under RLS and can CRUD limited to the tenant setting." do # rubocop:disable Metrics/LineLength
    skip "the test is not ready yet"
  end
end
